export function removeUserFromSubject(subjectId: string, userId: string) {
  // Example usage to avoid unused variable error
  console.log(`Removing user ${userId} from subject ${subjectId}`);
  // ...function logic...
}
// Remove user from subject (Person column)
// Remove user from subject (Person column)

